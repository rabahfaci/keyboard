# TEMPORARY: raw battery ADC debug over USB serial (CDC ACM), to verify
# whether the battery is actually charging (voltage trend) independent
# of any Bluetooth-reported percentage.
#
# REVERT once diagnosed:
#   git checkout -- boards/shields/temper/temper_left.conf
CONFIG_ZMK_USB_LOGGING=y
